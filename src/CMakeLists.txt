cmake_minimum_required(VERSION 2.8.6)

set_property(GLOBAL PROPERTY USE_FOLDERS ON)
set(CMAKE_CONFIGURATION_TYPES "Debug;Release")

PROJECT(anoncoind)

SET(macx:TARGET "anoncoind")

# Variables

SET(VERSION 0.8.7.2)

SET(OBJECTS_DIR obj)


